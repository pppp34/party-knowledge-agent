import { useState, useCallback } from 'react';
import { useAuth } from '../context/AuthContext';

// useAuth is re-exported from AuthContext for convenience
export { useAuth } from '../context/AuthContext';
